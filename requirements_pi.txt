# Raspberry Pi specific requirements
# Install these with: pip install -r requirements_pi.txt

# Core dependencies
flask==3.1.2
pypdf==6.0.0
soundfile==0.13.1
pydub==0.25.1

# PyTorch CPU version for Pi
torch==2.1.0 --index-url https://download.pytorch.org/whl/cpu

# TTS model - same version
kokoro>=0.9.2

# System dependencies to install via apt:
# sudo apt-get update
# sudo apt-get install -y espeak-ng ffmpeg python3-dev python3-pip
# sudo apt-get install -y libsndfile1-dev libffi-dev